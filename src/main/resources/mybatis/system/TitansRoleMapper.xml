<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.project.system.titansrole.mapper.TitansRoleMapper">

	<select id="selectRolesByCodes" resultType="com.ruoyi.project.system.titansrole.domain.TitansRole">
		select * from t_s_role
	<where>
		<if test="codeList != null and codeList.size() > 0">
			and code in
			<foreach collection="codeList" item="code" open="(" separator="," close=")">
				#{code}
			</foreach>
		</if>

		<if test="roleIdList != null and roleIdList.size() > 0">
			and roleid in
			<foreach collection="roleIdList" item="roleId" open="(" separator="," close=")">
				#{roleId}
			</foreach>
		</if>
		<if test="filter != null and filter != ''">
			and ${filter}
		</if>
	</where>
	</select>
	
</mapper> 